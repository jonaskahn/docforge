# `machine-config`

Content contract for document type `machine-config`.

Aliased with: `agents-kernel`, `fixed-shim` (same content contract).

| Type | Must present | Keep out | Primary mode | Depth |
|---|---|---|---|---|
| machine-config | compact entry points, verified commands, precedence, safe links to owning agent views | broad narrative, invented settings, or overwritten user configuration | Orientation | router |
