# `machine-config`

Content contract for document type `machine-config`.

| Type | Must present | Keep out | Primary mode | Depth |
|---|---|---|---|---|
| machine-config | self-contained valid settings with portable safety denials and only verified optional hooks, merged without discarding existing user keys | every documentation reference, invented commands, destructive allowances, host-specific or overwritten user configuration | Reference | reference |
