# Local preferences

This uncommitted file stores developer-specific Claude Code preferences for the
current checkout. Keep shared project behavior out of it, and never store
secrets, credentials, or local environment values here.
