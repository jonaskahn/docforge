# Commands

_Last reviewed: {{YYYY-MM-DD}}_

## `{{command}}`

| Flag | Long form | Description |
|---|---|---|
| `-{{x}}` | `--{{name}}` | {{description}} |

**Side effects:** {{writes a file / calls a service / mutates state / none}}

```bash
{{copy-pasteable example, no placeholders left}}
```

### Subcommands

#### `{{command}} {{subcommand}}`

{{same shape as above}}
