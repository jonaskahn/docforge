# Flow writing craft

Name the flow as a reader-recognizable outcome. Open with trigger, actors, and
result, then number the happy path in plain language. Put branches immediately
after the step that creates them. Use a diagram for multi-step or branching
flows. End with failures and recovery, keeping shared rules linked rather than
duplicated.
