{
  "_comment": "Template for docs/.docforge/manifest.json. Delete this key in real use. Rebuild with `python scripts/check_provenance.py --rebuild-manifest` rather than hand-editing once documents carry frontmatter.",
  "generated_at": "{{ISO-8601 timestamp of the last rebuild}}",
  "documents": {
    "{{docs/product/business-analyst/business-rules.md}}": {
      "sections": {
        "{{rule-or-flow-slug}}": {
          "sources": {
            "{{src/module/file.ext}}": "{{git hash-object output}}"
          }
        }
      }
    }
  }
}
