# `styling`

Content contract for document type `styling`.

Aliased with: `ui-components`, `browser-support` (same content contract).

| Type | Must present | Keep out | Primary mode | Depth |
|---|---|---|---|---|
| styling | component responsibilities, composition, tokens/themes, browser matrix, degradation | screenshot catalog or invented support claims | Reference | reference |
