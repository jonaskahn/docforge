# `model-card`

Content contract for document type `model-card`.

Aliased with: `model-lifecycle` (same content contract).

| Type | Must present | Keep out | Primary mode | Depth |
|---|---|---|---|---|
| model-card | datasets, training/evaluation, artifact lineage, inference, limitations, drift, ownership | unsupported quality or safety claims | Explanation | deep-dive |
