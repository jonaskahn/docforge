# `model-lifecycle`

Content contract for document type `model-lifecycle`.

Aliased with: `model-card` (same content contract).

| Type | Must present | Keep out | Primary mode | Depth |
|---|---|---|---|---|
| model-lifecycle | datasets, training/evaluation, artifact lineage, inference, limitations, drift, ownership | unsupported quality or safety claims | Explanation | deep-dive |
