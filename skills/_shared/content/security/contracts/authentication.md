# `authentication`

Content contract for document type `authentication`.

Aliased with: `api-reference`, `rate-limits` (same content contract).

| Type | Must present | Keep out | Primary mode | Depth |
|---|---|---|---|---|
| authentication | public surface, inputs/outputs, auth contract, limits, errors, compatibility source | hand-copied generated schema or secrets | Reference | reference |
