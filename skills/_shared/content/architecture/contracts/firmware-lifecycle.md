# `firmware-lifecycle`

Content contract for document type `firmware-lifecycle`.

Aliased with: `hardware-map` (same content contract).

| Type | Must present | Keep out | Primary mode | Depth |
|---|---|---|---|---|
| firmware-lifecycle | boards, peripherals, protocols, boot/update states, memory/power, failure | generic component datasheets | Explanation | deep-dive |
